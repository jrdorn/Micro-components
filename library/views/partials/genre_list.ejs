<h1><%= title %></h1>

<ul>

    <% if(Object.keys(genre_list).length > 0){ %>

        <% for (let index in genre_list) { %>

            <% let genre = genre_list[index]; %>

            <li><a href= <%- genre.url %> ><%- genre.name %></a> </li>

        <% } %>
    
    <% } else { %>
        <li>There are no genres.</li>
    <% } %>

</ul>